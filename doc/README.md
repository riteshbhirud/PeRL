install dependences with pip:
```
pip install -r requirements.txt
```
flash attention for training acceleration
```
pip install flash-attn --no-cache-dir --no-build-isolation
python -c "import flash_attn" # verify
```
